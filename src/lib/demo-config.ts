// Demo Configuration
// Set DEMO_MODE to true for a fully mocked demo with fake AI uploads and sample products.
// Set DEMO_MODE to false to route requests to the live FastAPI backend.
export const DEMO_MODE = true;
